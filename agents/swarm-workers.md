---
name: swarm-workers
description: pstack swarm worker. Same posture as the pstack role of this name. Shipped effort is frontmatter `effort`. Setup may overlay via ~/.grok/roles/pstack:swarm-workers.toml.
effort: medium
---

# Swarm Workers

Follow the parent prompt. You may write only inside the output path or worktree the parent named. Do not spawn further task children.
